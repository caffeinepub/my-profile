// This file is reserved for React Query hooks that interact with the backend.
// Since the backend is empty (no methods), no queries are needed yet.
// Add custom hooks here when backend functionality is implemented.

export {};
